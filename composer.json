{
  "name": "vercel/bridge-root",
  "private": true,
  "scripts": {
    "vercel": [
      "sh -lc 'mkdir -p perumahan-web/bootstrap/cache && chmod -R 0777 perumahan-web/bootstrap/cache && composer install -d perumahan-web --no-dev --prefer-dist --optimize-autoloader --no-interaction'"
    ]
  }
}
